📘 বাংলায় C++ প্রোগ্রামিং — শুরু থেকে অ্যাডভান্স পর্যন্ত
সূচিপত্র
ভূমিকা ও ইতিহাস

ডেভেলপমেন্ট সেটআপ

প্রথম প্রোগ্রাম

মৌলিক সিনট্যাক্স ও ডেটা টাইপ

অপারেটর

কন্ডিশনাল স্টেটমেন্ট

লুপ

ফাংশন

অ্যারে ও স্ট্রিং

পয়েন্টার ও ডাইনামিক মেমরি

স্ট্রাকচার ও এনাম

OOP (ক্লাস, অবজেক্ট, ইনহেরিটেন্স, পলিমরফিজম)

ফাইল হ্যান্ডলিং

STL

অ্যাডভান্স টপিক

প্রজেক্ট উদাহরণ

ইন্টারভিউ প্রস্তুতি ও প্র্যাকটিস

অধ্যায় 1 — ভূমিকা ও ইতিহাস
C++ একটি উচ্চ স্তরের প্রোগ্রামিং ভাষা যা Bjarne Stroustrup ১৯৮৩ সালে C ভাষার উপর ভিত্তি করে তৈরি করেন।
এটি General Purpose ভাষা, যেখানে সিস্টেম প্রোগ্রামিং, গেম, ডেস্কটপ সফটওয়্যার, মাইক্রোকন্ট্রোলার প্রোগ্রামিং, এবং আরও অনেক কিছু করা যায়।

অধ্যায় 2 — ডেভেলপমেন্ট সেটআপ
আপনি C++ লেখার জন্য নিচের সফটওয়্যার ব্যবহার করতে পারেন:

Code::Blocks

Dev-C++

VS Code (C++ Extension সহ)

g++ compiler (Linux এ sudo apt install g++)

অধ্যায় 3 — প্রথম প্রোগ্রাম
cpp
Copy
Edit
#include <iostream>
using namespace std;

int main() {
    cout << "হ্যালো, বিশ্ব!" << endl;
    return 0;
}
ব্যাখ্যা:

#include <iostream> → ইনপুট/আউটপুট লাইব্রেরি

using namespace std; → std:: না লিখেও cout/cin ব্যবহার

main() → প্রোগ্রামের শুরু

cout → আউটপুট দেখায়

অধ্যায় 4 — মৌলিক সিনট্যাক্স ও ডেটা টাইপ
ডেটা টাইপ উদাহরণ:

cpp
Copy
Edit
int age = 21;
float height = 5.8;
char grade = 'A';
bool passed = true;
string name = "Rahim";
অধ্যায় 5 — অপারেটর
Arithmetic: + - * / %

Relational: == != > < >= <=

Logical: && || !

Assignment: = += -= *= /=

Bitwise: & | ^ << >>

অধ্যায় 6 — কন্ডিশনাল স্টেটমেন্ট
cpp
Copy
Edit
if (age >= 18) {
    cout << "প্রাপ্তবয়স্ক";
} else {
    cout << "অপ্রাপ্তবয়স্ক";
}
অধ্যায় 7 — লুপ
for loop:

cpp
Copy
Edit
for (int i = 1; i <= 5; i++) {
    cout << i << endl;
}
while loop:

cpp
Copy
Edit
int i = 1;
while (i <= 5) {
    cout << i << endl;
    i++;
}
অধ্যায় 8 — ফাংশন
cpp
Copy
Edit
int add(int a, int b) {
    return a + b;
}

int main() {
    cout << add(3, 4);
}
অধ্যায় 9 — অ্যারে ও স্ট্রিং
cpp
Copy
Edit
int nums[5] = {1, 2, 3, 4, 5};
string text = "C++ শেখা মজা";
অধ্যায় 10 — পয়েন্টার ও ডাইনামিক মেমরি
cpp
Copy
Edit
int x = 10;
int* ptr = &x;
cout << *ptr; // 10
ডাইনামিক মেমরি:

cpp
Copy
Edit
int* arr = new int[5];
delete[] arr;
অধ্যায় 11 — স্ট্রাকচার ও এনাম
cpp
Copy
Edit
struct Student {
    string name;
    int age;
};

enum Color {Red, Green, Blue};
অধ্যায় 12 — OOP
cpp
Copy
Edit
class Car {
public:
    string brand;
    void drive() {
        cout << brand << " is driving";
    }
};

int main() {
    Car c1;
    c1.brand = "Toyota";
    c1.drive();
}
অধ্যায় 13 — ফাইল হ্যান্ডলিং
cpp
Copy
Edit
#include <fstream>
ofstream file("data.txt");
file << "Hello File";
file.close();
অধ্যায় 14 — STL
cpp
Copy
Edit
#include <vector>
vector<int> v = {1, 2, 3};
v.push_back(4);
অধ্যায় 15 — অ্যাডভান্স টপিক
Template

Exception Handling

Multithreading

Smart Pointer

অধ্যায় 16 — প্রজেক্ট উদাহরণ
Calculator App

Student Management System

File-based Phonebook

Tic Tac Toe Game

অধ্যায় 17 — ইন্টারভিউ প্রস্তুতি
C++ vs C

OOP Concepts

Pointer & Memory

File Handling

STL Basics

